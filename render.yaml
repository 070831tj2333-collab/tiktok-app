# Render Blueprint: static Vite build + SPA fallback for React Router
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: tiktok-app
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
